name: CI

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Hugo setup
        uses: peaceiris/actions-hugo@v3.0.0
        with:
          extended: true
      - name: Build
        run: hugo --minify
